<%= form_with(model: flr, local: true) do |form| %>
  <% if flr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flr.errors.count, "error") %> prohibited this flr from being saved:</h2>

      <ul>
      <% flr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
